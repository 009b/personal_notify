# Архитектура: personal_notify

> Статус: ЧЕРНОВИК — уточняется после финализации SPEC.md

## Общая схема

```
┌─────────────────────────────────────────────┐
│               Telegram API                  │
└────────────────┬────────────────────────────┘
                 │ polling / webhook
┌────────────────▼────────────────────────────┐
│                  Bot (aiogram)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ handlers │  │ scheduler│  │ webhooks │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       └─────────────┼─────────────┘        │
│              ┌──────▼──────┐               │
│              │   services  │               │
│              └──────┬──────┘               │
│              ┌──────▼──────┐               │
│              │   storage   │               │
│              └─────────────┘               │
└─────────────────────────────────────────────┘
```

## Компоненты

### bot/ — основной пакет
| Модуль           | Назначение                          | Статус  |
|------------------|-------------------------------------|---------|
| `main.py`        | Точка входа, запуск polling         | TODO    |
| `config.py`      | Конфигурация из .env                | TODO    |
| `handlers/`      | Обработчики команд и сообщений      | TODO    |
| `services/`      | Бизнес-логика уведомлений           | TODO    |
| `models/`        | Модели данных                       | TODO    |
| `scheduler.py`   | Планировщик напоминаний (cron)      | TODO    |
| `webhook.py`     | Приём внешних событий               | TODO    |

## Хранилище данных

<!-- TODO: выбрать после финализации SPEC -->
- Вариант A: SQLite + aiosqlite (просто, без зависимостей)
- Вариант B: PostgreSQL + asyncpg (если нужна надёжность)

## Деплой

<!-- TODO: определить после финализации SPEC -->
- Вариант A: systemd-сервис на VPS
- Вариант B: Docker-контейнер
